Python jest językiem TYPOWANYM DYNAMICZNIE.
Oznacza to, że podczas przypisywania WARTOŚCI do ZMIENNEJ nie musimy deklarować TYPU DANYCH (Python sam zarządzi miejscem w pamięci).
Pod jedną zmienną możemy podstawiać różne typy danych.

liczba = 4
liczba = 'cztery'

FUNKCJE WBUDOWANE:
    -> print() – Wyświetl zawartości w konsoli. WARTOŚCI zostaną wyświetlone w jednym wierszu, jeśli zostaną oddzielone przecinkami.

    -> type() – Sprawdź typ zmiennej.
    -> isinstance(value, class) - Sprawdza czy dana WARTOŚĆ jest danego typu (klasy), np. insinstance(number, int) -> zwraca bool (True lub False)

    -> open(filepath, mode, encoding) – łuży do zapisu / odczytu pliku
        Najczęściej używane TRYBY funkcji open()
        – 'r' (read) – Tryb ODCZYTU (domyślny)
        – 'w' (write) – Tryb ZAPISU
        – 'a' (append) – Tryb DOPISANIA.

        with open('data/jakis_plik.txt', encoding='utf-8') as file: # Gdzie "file" to dowolny wyraz
            content = file.read()
            print (content)

    -> input() – Pobierz WARTOŚĆ od użytkownika z konsoli. Zawsze zwraca STRING, dlatego czasem trzeba dokonać konwersji na "int" lub "float".
            To co wpisze użytkownik jako "input", zostanie przypisane do WARTOŚCI, a następnie np. może zostać wyświetlone za pomocą funkcji "print".
            Docelowo nie będziemy używali tej funkcji. Dane będziemy pobierali z innych źródeł, np. źródłem może być interface.
    -> len(iterable) – Zwraca długość obiektu ITEROWALNEGO (czyli takiego, z których da się wyodrębnić konkretny element lub kilka elementów).
            Obiektami iterowalnymi są: STRINGI, LISTY, TUPLE, ZBIORY i SŁOWNIKI.
    -> round(number) – Zaokrągla domyślnie do jedności, ale też pozwala zaokrąglić do X miejsc o przecinku.
            round(number, precision) – Zaokrąglenie number (liczby zmiennoprzeciwkowej) do precision (dokładność) liczby po przecinku
            -> number – Argument pozycyjny. Potrzebny, aby podana funkcja zadziałała.
            -> precision – Argument opcjonalny. Ma wartość domyślną, można, ale nie trzeba go podawać, np: round(number, 2) – zaokrągli do 2 miejsc o przecinku.

    -> sorted(collection) -> sortuje obiekt KOLEKCJI -> zawsze zwraca LISTĘ (w kolejności wg. ASCI)
            sorted(iterable, /.., *.., key=none, reverse=False) # W argumencie "key" wpisujemy jakiej FUNKCJI chcemy użyć, żeby posortować.

    -> open(filepath, mode, encoding) -> służy do zapisu / odczytu pliku

    -> reversed(collection) -> sortuje obiekt KOLEKCJI w odwrotnej kolejności -> zawsze zwraca LISTĘ
    -> min(sequence) -> zwraca najmniejsza wartość z KOLEKCJI
    -> max(sequence) -> zwraca największą wartość z KOLEKCJI

    -> str(a) – Zamiana na typ danych NAPIS/CIĄG ZNAKÓW (str)
    -> int(a) – Zamiana na typ danych LICZBA CAŁKOWITA (int)
            Jeśli chcesz zamienić NAPIS (str) na LICZBĘ (int/float), to musi się on składać wyłącznie z CYFR. W innym wypadku wyświetli się błąd.
    -> float(a) - Zamiana wartości na LICZBĘ ZMIENNOPRZECINKOWĄ (float)
            Jeśli chcesz zamienić NAPIS (str) na float, to musi się on składać wyłącznie z CYFR lub może zawierać KROPKĘ (jest separatorem dziesiętnym)
            np. ze str = '123.12' można przejść na int za pośrednictwem float – Konwersja: str -> float -> int
    -> bool() – Konwersja danych na TYP LOGICZNY

    -> id() – Zwraca unikalny, całkowitoliczbowy identyfikator obiektu. W praktyce jest to adres tego obiektu w pamięci komputera.
            Każdy obiekt ma przypisane własne id w momencie jego tworzenia (id jest swego rodzaju "pseudo" liczbą/pozycją).
            w praktyce, używamy jej jedynie do sprawdzenia, czy coś jest HASHOWALNE.

    -> pow(a, b) – Potęgowanie a do potęgi b – NIE UŻYWAMY, ponieważ mamy operator **.

TYPY DANYCH W PYTHONIE:
    -> Numeryczne (typy proste)
        -> Integers (int): LICZBA całkowita, np. 2 lub -2. Liczby całkowite mogą być dowolnie duże (lub małe) i mogą przyjmować wartości ujemne.
        -> Floats (float): LICZBA zmiennoprzecinkowa (z ułamkiem), np. 2.5.

    -> Teksty (typ prosty)
        - Strings (str): TEKST, zawsze zapisywane w cudzysłowie (pojedynczym lub podwójnym), np. "Dwa" lub '2.5'.
        Każda treść umieszczona w cudzysłowie jest traktowana jako TEKST/STRING.

            -> METODY stringów (nie mylić z FUNKCJAMI numerycznymi) – metoda jest wywoływana na konkretnym obiekcie/instancji
                Dla "int" nie ma takich METODY – wyświetli błąd
            – Najczęściej wykorzystywane METODY stringów to:
                .split(',') – zwraca listę str / .strip() – usuń białe znaki / .lstrip() / .rstrip() /
                ''.join() – połącz w str (w '' podaj separator np. ',') / .replace('a', 'b') / .startswith() / .endswith()
                .upper() / .lower() / .title() / .capitalize() / .isaplha() / .isdigit() / isnumeric() – ½ + 三
            – Żadziej wykorzystywane: strip() /
                .find() – zwraca index / .index() – zwraca index lub BŁĄD / .count() – zwraca liczbę wystąpień /

    -> Wartości logiczne (typy proste)
        – Booleans (bool): Wartości logiczne, czyli prawda (True) lub fałsz (False). Muszą zaczynać się wielką literą

    -> Struktury Danych (typ złożony) i ich METODY
        – Listy [list]: Tworzenie listy: <zmienna> = list(<wartość_1, <wartość<2>> / Inny przykład: all_letters = list(string.ascii)
            list[0] / list[0:3] / list.index('Aga') / list.remove('Aga') / list.append('Aga') / list.insert(0, 'Aga') / list[0] = 'Ewa'
            for element in list:

        – Krotki (tuple): tuple[0] / tuple[0:3] / tuple.index('Aga')
            for element in tuple:

        – Zbiory {set}: set.remove('Aga') / set.add('Aga') /
            (s1 - s3) – odejmowanie ZBIORU od ZBIORU / (s1 & s3) – część wspólna / (s1 ^ s3) – część rozłączna
            Nie możesz modyfikować elementu, ale możesz najpierw usunąć, a później dodać nowy.

        – Słowniki [dict]: dict.get('name') – wyświetl ELEMENT słownika / dict.pop('name')  – usuwanie / dict.get('name')
            dict['name'] = 'Aga' – dodawanie i edycja (dodajac istniejący klucz edytuje wartość)
            Iteracja: for key in dict.keys(): / for value in dict.values() / for key, value in dict.items():
                            print(key)              print(value)                print(f' Klucz: {key} + Wartość: {value}'))

    -> None – Posiada swój własny, unikalny typ o nazwie "NoneType". Jest on oddzielony od LICZB, TEKSTÓW i WARTOŚCI LOGICZNYCH.


SŁOWA KLUCZOWE
    – raise – Podnosimy BŁĄD. Można użyć w FUNKCJI np. raise TypeError('Liczba musi być całkowita.')


MODUŁU będące częścią biblioteki standardowej języka Python.
        MODUŁY to zbiory gotowych funkcji i zmiennych (stałych).
        Pozwalają unikać pisania wszystkiego od zera i ułatwiają organizację kodu

    -> import random – MODUŁ umożliwia losowanie ZNAKÓW z danego przedziału.
        – random.randint(a, b) # Losuje jedną (pseudo)losową liczbę całkowitą (a i b to początek i koniec zakresu)
            – random.seed(<wartość>) # Ziarno losowe dla powtórzenia wyników losowania (powtórzenie wyniku losowania).
                                     Wartością musi być dowolny INTEGER.
        – random.shuffle(x) / sample(x, k=len(x))
        – random.choices(<wartość>, k=30) # Losuje powtarzające się znaki
        – random.sample(<wartość>, k=30) # Losuje NIE powtarzające się znaki

    -> import string – MODUŁ, który zwraca wszystkie ZMIENNE/ZNAKI określonego typu.
        – print(string.ascii_letters) # Zwróci wszystkie litery: abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ
        – print(string.ascii_lowercase) # Zwróci wszystkie małę litery: abcdefghijklmnopqrstuvwxyz
        – print(string.ascii_uppercase) # Zwróci wszystkie DUŻE litery: ABCDEFGHIJKLMNOPQRSTUVWXYZ
        – print(string.digits) # Zwróci wszystkie litery: 0123456789
        – print(string.punctuation) # Zwróci wszystkie znaki specjalne: !"#$%&'()*+,-./:;<=>?@[\]^_`{|}~

    -> import math – MODUŁ,

    -> import datetime – MODUŁ,
        now_str = datetime.now().strftime('%Y.%m.%d %H:%M:%S')
            file.write(f'Aktualna data: {now_str}')

    -> import date – MODUŁ,

    -> import os – MODUŁ,
        FUNKCJE:
        - os.getcwd – Zwróci aktualną ŚCIEŻKĘ ROBOCZĄ katalogu (Get current working directory)
        – os.listdir – Zwróci LISTĘ NAZW plików z katalogu. Jako argument przyjmuje NAZWĘ katalogu.


Tworzenie i aktywacja ŚRODOWISKA WIRTUALNEGO w Pythonie

– Otwieramy wiersz poleceń:
    – Wchodzimy to interesującego nas katalogi i tworzymy w nim folder
        mkdir proj_data_science
        mkdir proj_cv
    – Wchodzę do każdego z tych folderów i tworzę dla nich środowisko wirtualne
        1. Utworzenie środowiska (wywołaj polecenia:)
            python -m venv my_env_cv # Gdzie my_env to [własna_nazwa_srodowiska].
            Wyodrębniamy z globalnego Pythona "małego" Pythona
        2. W katalogu roboczym projektu powinien utworzyć się folder z nazwą środowiska, a w nim Scripts lub bin...
        3. AKTYWACJA środowiska (uruchomienie pliku activate)
            my_env\scripts\activate # dla Windows
            source my_env_cv/bin/activate # dla Linux / MacOS
            Środowisko jest aktywne, gdy na początku promptu w terminalu widoczna jets jego nazwa w nawiasie.
        4. Kiedy mam już aktywne środowisko mogę sprawdzić, w którym Pythonie pracuje.
            import sys
            sys.executable
        5. Dezaktywacja środowiska
            deactivate

Zarządzanie środowiskiem wirtualnym
Będziemy nim zarządzać przy pomocy wbudowanego managera PIP – wbudowanego w Pythonie.

    –> PAMIĘTAJ, aby mieć aktywne środowisko wirtualne, wykonując wszystkie poniższe polecenia.

    –> WYLISTOWANIE zainstalowanych pakietów (zainstalowane środowisko będzie puste – pomijając pip)
        pip list

    –> INSTALACJA pakietów (z pypi.org) – Przez polecenie
        Sprawdź, czy masz aktualną wersję pip i zrob ewentualny ubgrade.
        python.exe -m pil install --upgrade pip

        pip install [nazwa_pakietu]
            np.:
            pip install numpy (polecenie znajduje się na stronie z danym pakietem)

        Przykład użycia zainstalowanej funkcji
        import numpy as np
        np.array([1,2,3,4]) * 10
        quit # Wychodzę z funkcji
        WAŻNE! Jeżeli dezaktywuje numpy, to nie będzie on zainstalowany dla Pythona globalnego

    -> Instalowanie konkretnej WERSJI pakietu
        pip install [nazwa_pakietu]==wersja # np. pip install numpy==2.5.0
        Jeżeli chciałbym zainstalować mog ęto zrobić.
        W danym środowisku wirtualnym można znajdować się tylko jedna wersja pakietu.
        Jeżeli zainstalujemy inną wersję, to stara zostanie usunięta i zainstalowana nowa (nadpisana).

    -> Instalowanie wielu bibliotek jednocześnie ()
        pip install [pakiet_1] [pakiet_2] [pakiet_3] # np. pip install numpy scipy fastapi
        WAŻNE! Niektóre pakiety będą wymagały instalacji innych pakietów.
        pip list # aby sprawdzić, co się zainstalowało

    -> Odinstalowywanie pakietu
        pip uninstall [nazwa_pakietu] # np. pip uninstall numpy

    -> Tworzenie środowiska wirtualnego NA BAZIE innego środowiska
        python -m venv my_venv

    Django narzuca strukturę projektu. Dobrze jest od niego zacząć, kiedy myślimy o BackEnd!

UWAGA! Cały folder ze środowiskiem zawsze go ignorujemy jeśli chodzi o GitHub (nie będziemy go trackować)
    Generowanie i zapis zależności ze środowiska wraz z ich wersjami (jeżeli chcę udostępnić komuś moje środowisko).
    pip freez > requirements.txt
    1. Tworzę kopię swojego projektu: cd project_1 copy.
    2. Tworzę środowisko wirtualne (w oparciu o tego samego Pythona).
    3. Zamiast instalować wszystkie pakiety, kopiuje do katalogu utworzone wcześniej requirements.txt
    4. Akgtywuje środowisko my_venv\scripts\activate
    5. Instaluję komplet elementów z pliku requirements.txt: pip install -r requirements.txt # Flaga "-r" -> READ
